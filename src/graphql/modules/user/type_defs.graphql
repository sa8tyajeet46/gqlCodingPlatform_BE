type User {
  id: ID!
  name: String!
  email: String!
  role: String!
}

type Mutation {
  signup(
    name: String!
    email: String!
    password: String!
  ): User!

  login(
  email: String!
  password: String!
  ): User!
}

type Query {
me: User!
logout: String!
}
